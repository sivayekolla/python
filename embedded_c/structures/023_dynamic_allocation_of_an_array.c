/*
struct Block {
    int n;
    int *values;
};
struct Block *blocks = malloc(10 * sizeof(struct Block));
*/

//decloring a structure pointer and using dynamic
//memory allocation to variable we can asssign memory.